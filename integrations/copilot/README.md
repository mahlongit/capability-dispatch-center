# GitHub Copilot

CD-Center works as a local routing layer before Copilot chat or coding sessions.

Recommended flow:

```bash
cd-center scan
cd-center open
```

Then copy the selected prompt into Copilot Chat.
